import React from 'react';

const TileCard = ({tile}) => {

    const {
        id,
        title,
        description,
        image,
        category,
        price,
        currency,
        dimensions,
        material,
        inStock
    } = tile;

    return (
        <div>
            
        </div>
    );
};

export default TileCard;